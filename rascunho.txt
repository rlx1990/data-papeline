📊 Data Pipeline – FakeStore API → Data Warehouse
🎯 Objetivo do Projeto

Construir um pipeline de dados ponta a ponta, realizando:

Ingestão de dados via API

Armazenamento em camada raw (staging)

Persistência em formato analítico (Parquet)

Preparação para modelagem dimensional (DW)

O projeto simula um cenário real de engenharia de dados.

🏗 Arquitetura Atual
FakeStoreAPI
     ↓
Ingestão (Python)
     ↓
Arquivos JSON (raw)
     ↓
Conversão para Parquet
     ↓
PostgreSQL (Schema: staging)


Fonte de dados:

Fake Store API

Banco utilizado:

PostgreSQL

🔄 Fluxo do Pipeline
1️⃣ Ingestão

Consumo dos endpoints:

products

users

carts

Dados salvos como JSON em:

data/raw/fakestore/

2️⃣ Padronização e Armazenamento Analítico

Conversão dos arquivos JSON para formato Parquet

Padronização de nomes de colunas (lowercase + underscore)

Objetivo:

Preparar dados para análises futuras

Otimizar armazenamento

3️⃣ Camada Staging (Raw Layer)

Criação do schema:

staging


Tabelas criadas:

staging.products_raw
staging.users_raw
staging.carts_raw


Estrutura padrão:

id SERIAL PRIMARY KEY
payload JSONB
dt_ingest TIMESTAMP DEFAULT CURRENT_TIMESTAMP


Características:

Armazena o dado exatamente como recebido

Não há transformação nessa camada

Permite reprocessamento

Garante rastreabilidade

Essa camada representa a Bronze Layer da arquitetura moderna.

🧠 Conceitos Aplicados

✔ Ingestão via API
✔ Manipulação de JSON
✔ Conversão para Parquet
✔ Persistência em banco relacional
✔ Uso de JSONB
✔ SQL dinâmico seguro com psycopg2
✔ Arquitetura em camadas
✔ Separação entre Raw e Transformação

🛠 Stack Tecnológica

Python

requests

pandas

psycopg2

PostgreSQL

SQL

📂 Estrutura do Projeto
data-pipeline/
│
├── data/
│   └── raw/
│       └── fakestore/
│           ├── products_YYYY-MM-DD.json
│           ├── users_YYYY-MM-DD.json
│           ├── carts_YYYY-MM-DD.json
│           ├── products_YYYY-MM-DD.parquet
│           ├── users_YYYY-MM-DD.parquet
│           └── carts_YYYY-MM-DD.parquet
│
├── src/
│   └── ingestion/
│       └── ingest_api.py
│
└── README.md

🚀 Status Atual do Projeto

✔ Ingestão funcional
✔ Armazenamento raw implementado
✔ Persistência em staging validada
✔ Conexão com banco testada
✔ Dados carregados com sucesso

Próxima etapa:

Transformação da camada staging

Criação de modelo dimensional

Construção de dimensões e tabela fato

🎓 Objetivo Técnico do Projeto

Simular um pipeline real de engenharia de dados aplicando:

Arquitetura em camadas

Boas práticas de ingestão

Persistência confiável

Base para modelagem dimensional